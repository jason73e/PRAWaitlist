@model IEnumerable<PRAWaitList.Models.ConfigurationSettingsModel>

@{
    ViewBag.Title = "Index";
}

<h2>Configuration</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.key)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.key)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.key }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.key })
        </td>
    </tr>
}
    <tr>
        <td>
            @Html.ActionLink("Add Seed Values", "Seed", null, new { @class = "btn btn-primary btn-success" })
        </td>
        <td></td>
        <td></td>
    </tr>

</table>
