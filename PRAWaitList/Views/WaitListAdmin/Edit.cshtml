@model PRAWaitList.Models.IntentToEnrollViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
        <div class="panel panel-default">
            <div class="panel-body">
                @Html.Partial("EditFamily", new ViewDataDictionary(ViewData) { TemplateInfo = new TemplateInfo() { HtmlFieldPrefix = "Family" } })
                @Html.Partial("EditStudent", new ViewDataDictionary(ViewData) { TemplateInfo = new TemplateInfo() { HtmlFieldPrefix = "Student" } })
                @Html.Partial("EditParents", new ViewDataDictionary(ViewData) { TemplateInfo = new TemplateInfo() { HtmlFieldPrefix = "Parents" } })
                @Html.Partial("EditSiblings", new ViewDataDictionary(ViewData) { TemplateInfo = new TemplateInfo() { HtmlFieldPrefix = "Siblings" } })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" name="Save" class="btn btn-primary" />
            </div>
        </div>
}
<div>
    @Html.ActionLink("Back to List", "Index")
</div>

